# Copier ce fichier vers /home/arnaud/apps/proba-loto-euromillions/.env
# puis adapter toutes les valeurs avant un deploiement "home".

# Base de donnees
POSTGRES_USER=probaloto
POSTGRES_PASSWORD=change-me-strong-db-password

# Admin
ADMIN_API_KEY=change-me-strong-admin-api-key
ADMIN_WEB_USERNAME=admin
ADMIN_WEB_PASSWORD=change-me-strong-admin-password

# Liens publics utilises dans les e-mails de confirmation/desinscription
PUBLIC_BASE_URL=https://loto.arnaudwissart.fr
SUBSCRIPTIONS_TOKEN_SECRET=change-me-very-long-random-secret

# SMTP (obligatoire pour que l'abonnement fonctionne)
SMTP_HOST=smtp.votre-fournisseur.tld
SMTP_PORT=587
SMTP_USER=votre-utilisateur-smtp
SMTP_PASS=votre-mot-de-passe-smtp
SMTP_FROM=Probabilites Loto & EuroMillions <no-reply@arnaudwissart.fr>
SMTP_USE_STARTTLS=true

# Optionnel: active le check SMTP dans /health/ready
HEALTHCHECKS_SMTP_ENABLED=true

# Optionnel: origine CORS supplementaire
# CORS_ALLOWED_ORIGIN_1=https://loto.arnaudwissart.fr
